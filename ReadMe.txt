*Requirements:
--python v 3.6 or above

*Instructions to run the application via CMD:
 --open cmd 
 --active the project directory ==>  cd "project path"
 --run virtual environment ==> cd venv/Scripts
 --active the virtual environment using 'activate' command.
 --back to the project directory.
 --run main.py 


**note:
 make sure the 2 images place in the "img" folder and name as bellow:
  main image(fixed) ==>  1.png
  2nd image ==========>  2.png
 
